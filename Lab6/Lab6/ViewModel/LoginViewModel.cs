namespace Lab6.ViewModel
{
    internal class LoginViewModel : BaseViewModel
    {
        //public ICommand SwitchToRegisterCommand { get; }

        //public ICommand LoginCommand { get; }

        ////void SwitchToRegister()
        ////{
        ////    if (ParentViewModel is WindowViewModel parent)
        ////    {
        ////        parent.CurrentViewModel = new RegisterViewModel(parent);
        ////    }
        //}

        //void Login()
        //{

        //}

        //public LoginViewModel(BaseViewModel? parentViewModel = null) : base(parentViewModel)
        //{
        //    SwitchToRegisterCommand = new DelegateCommand(SwitchToRegister);
        //    LoginCommand = new DelegateCommand(Login);
        //}
    }
}
